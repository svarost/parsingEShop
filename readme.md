### Description

My project. The project is created for useful to do.

### Requirements

Before first start necessary install base requirements your project from file `requirements.txt`

Step:
1. cd to target dir
2. execute `pip install -r requirements.txt`

### Commands

Run:

`python main.py`

Start:

`Ctrl + C`